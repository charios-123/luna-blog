server:
  port: 8890
  mode: release # debug, release, test

database:
  host: 127.0.0.1
  port: 3306
  user: luna_blog
  password: zswbrbaFirWCJXTW
  dbname: luna_blog
  charset: utf8mb4

jwt:
  secret: olsNI/ke8Z4XujzC/4TWeQGAWBifoJs7MiVW/DEZF9o=
  expire: 24 # hours

oss:
  endpoint: oss-cn-hangzhou.aliyuncs.com
  access_key_id: xxxx
  access_key_secret: xxxxx
  bucket_name: dycloud-leaf
  base_url: https://xxxxxx.oss-cn-hangzhou.aliyuncs.com

redis:
  host: 127.0.0.1
  port: 6379
  password:
  db: 0
  pool_size: 10

# AI 能力配置(DeepSeek 官方 OpenAI 兼容接口)
ai:
  provider: deepseek # AI 服务商标识
  base_url: https://api.deepseek.com # DeepSeek 官方接口
  api_key: "" # 通过环境变量 AI_API_KEY 注入,勿写入本文件
  model: deepseek-chat # DeepSeek-V3 系列对话模型
  timeout: 60 # 单次请求超时(秒)
  max_content_chars: 8000 # 发送给模型的文章正文最大字符数

# 天气配置(和风天气,key 通过环境变量 QWEATHER_API_KEY 注入)
weather:
  api_key: "" # 通过环境变量 QWEATHER_API_KEY 注入,勿写入本文件
  host: "https://p45n8phtfy.re.qweatherapi.com" # 和风控制台分配的 API Host
  city: "新乡" # 城市名(中文或拼音),如 广州 / guangzhou

log:
  level: info # debug, info, warn, error
  format: text # json, text
  output: stdout # stdout, file
  file_path: logs/app.log
  max_size: 100 # MB
  max_backups: 3
  max_age: 7 # days
